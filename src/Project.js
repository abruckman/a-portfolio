import React, {Component} from 'react';
import {Card, CardHeader, CardText} from 'material-ui/Card';
import PropTypes from 'prop-types';

class Project extends Component{

  render(){
    const features = this.props.project.features
  }

}
